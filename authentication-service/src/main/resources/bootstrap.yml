spring:
  application:
    name: authentication
  profiles:
    active: local
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: discovery
      fail-fast: true


eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/