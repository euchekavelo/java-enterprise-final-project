server:
  port: 8081


spring:
  application:
    name: gateway
  profiles:
    active: local
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: discovery
      fail-fast: true


eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/